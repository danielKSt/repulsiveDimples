% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trust_step_quadratic.R
\name{ess_ray_limit}
\alias{ess_ray_limit}
\title{Largest feasible step along a ray}
\usage{
ess_ray_limit(
  x_0,
  dir,
  t_hi,
  trust_function,
  eta_trust,
  nSims,
  bisection_iterations,
  state
)
}
\arguments{
\item{x_0}{Centre of the trust region, which always passes: every importance sampling
weight against its own ensemble is 1, so the effective sample size there is \code{nSims}.}

\item{dir}{Direction of the ray. Not required to be a unit vector; \code{t} is in units of
\code{dir}.}

\item{t_hi}{Largest \code{t} to consider.}

\item{trust_function}{Trust function, returning a list with \code{f_est} and \code{ess}.}

\item{eta_trust}{Smallest \code{ess/nSims} an iterate is allowed to have.}

\item{nSims}{Number of simulations the ensemble behind \code{trust_function} holds.}

\item{bisection_iterations}{Bisection steps to take once the far end has failed.}

\item{state}{A list with \code{ess_bounds} and \code{evals}, threaded through the step.}
}
\value{
A list with \code{t} (the largest value found to pass, 0 if none did) and the
updated \code{state}.
}
\description{
Finds how far \code{x_0 + t*dir} can go, for \code{t} in \verb{(0, t_hi]}, before \code{ess/nSims} drops to
\code{eta_trust}. The far end is tried first, so a ray that is feasible all the way to
\code{t_hi} costs one evaluation rather than a full bisection -- which is the common case on
a coordinate where the trust radius binds before the importance sampling does.

Bisection is the right search here because the effective sample size falls off
monotonically as a point moves away from the parameters the ensemble was simulated
from, so the ray crosses the requirement exactly once. Measured on study1's starting
parameters at \code{nSims = 300}, \code{ess/nSims} on a 13-point grid across \verb{+/- 0.2} was
monotone decreasing outwards on both sides of all three coordinate axes, with a single
crossing per side.
}
