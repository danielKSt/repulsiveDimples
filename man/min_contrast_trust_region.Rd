% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/min_contrast_trust_region.R
\name{min_contrast_trust_region}
\alias{min_contrast_trust_region}
\title{Complete estimation procedure using importance sampling + simulation + minimum contrast}
\usage{
min_contrast_trust_region(
  params,
  parFreeIndex,
  repRange,
  rho_hat,
  K_hat,
  xlims,
  ylims,
  nSims,
  deltaInit,
  eta,
  deltaMax,
  deltaMin = 1e-04,
  wq = c(1000, 1/4),
  normalized = TRUE,
  eta_trust = 0.6,
  eta_converged = 0.99,
  validate.converged = FALSE,
  subsection_count.main = 11,
  subsection_count.prefit = 11,
  line_iterations.main = 4,
  line_iterations.prefit = 4,
  max.directions.main = NULL,
  max.directions.prefit = 1,
  carry.directions = FALSE,
  carry.ess_bounds = TRUE,
  method.main = c("conjugate", "quadratic"),
  method.prefit = c("conjugate", "quadratic"),
  interp_fraction.main = 0.25,
  interp_fraction.prefit = 0.25,
  bisection_iterations.main = 10,
  bisection_iterations.prefit = 10,
  tolPrefitLoops = 0.05,
  maxPrefitLoops = 6,
  max.iter.prefit = 10,
  tol = 10^-8,
  max.iter = 1000,
  printProgress = FALSE
)
}
\arguments{
\item{params}{Initial guess for parameters to estimate}

\item{parFreeIndex}{Indices of parameters to estimate}

\item{repRange}{Repulsion range}

\item{rho_hat}{Estimated intensity for data}

\item{K_hat}{Estimated K-function for data}

\item{xlims}{Simulation window x limits}

\item{ylims}{Simulation window y limits}

\item{nSims}{Number of simulations}

\item{deltaInit}{Initial trust radius}

\item{eta}{Trust region parameter}

\item{deltaMax}{Greatest trust region radius allowed}

\item{deltaMin}{Convergence tolerance for trust region radius}

\item{wq}{Weights for contrast function}

\item{normalized}{Normalize IS weights?}

\item{eta_trust}{Smallest effective sample size, as a fraction of \code{nSims}, an iterate
proposed by \code{\link{trust_step}} is allowed to have. Between 0 and 1.}

\item{eta_converged}{Effective sample size, as a fraction of \code{nSims}, at or above
which the fit is taken to have converged: once the iterate \code{\link{trust_step}}
proposes is still this well supported by the ensemble simulated at the current
parameters, the importance sampling has not been stretched and there is nothing left
to re-simulate for. Expected to be larger than \code{eta_trust}, and in practice close to 1.

A high effective sample size at the proposed iterate says the importance sampling is
still reliable there; it does not say the contrast has been minimised, and on study1
those come apart. Sweeping this over 0.8/0.9/0.95/0.99/0.999 and scoring each fit's
endpoint against a \emph{fresh} 400-pattern ensemble gives mean contrasts of 2.49, 1.85,
1.27, 1.02 and 0.64 against 5.32 at the starting point: the more readily this fires,
the worse the fit. Judge it that way and not on the returned \code{f_vals}, whose last entry
at an ESS-converged step is the minimum of the importance sampling surface taken over
the same ensemble that defines it, and so is optimistic by up to an order of magnitude
(0.143 reported against 2.49 measured, at 0.8).

Measured on the parameters rather than on the contrast, the same setting matters much
less: 50 paired fits at \code{nSims = 500} differing only in this argument put the mean L2
error in log parameters at 0.625 for 0.9 against 0.663 for 0.99, with 0.99 ahead in 19
of the 50 pairs (paired p = 0.15) and costing 1.4 times the wall time. What those runs
do settle is where the error comes from. Bias dominates variance about 3:1 in every
parameter, and a paired check at \code{nSims = 5000} put the contrast at 0.0384 at the true
parameters against 0.2372 at the median fitted point (15 reps, p = 0.007): the contrast
surface prefers the truth, and the optimizer stops short of it. A disappointing fit is
therefore evidence about how hard \code{\link{trust_step}} searched -- \code{max.directions},
and which \code{method} it used -- rather than about the contrast function or its weights.}

\item{validate.converged}{What to do with the iterate that triggers \code{eta_converged}.
\code{FALSE} (the default) accepts it straight from the importance sampling surface and
stops, never simulating there. \code{TRUE} puts it through the same fresh simulation and
\code{\link{evaluate_improvement}} test as any other step, and only then stops, so the
step can still be rejected.

The two are worth comparing because they trade different errors. Not validating avoids
feeding a second ensemble's sampling error into the decision, but leaves \code{f_vals}
ending on \code{f_pred}, which is the smallest of several hundred evaluations taken over the
ensemble that selected it and is optimistic by roughly an order of magnitude at small
\code{nSims} -- and, measured on study1, does not get better as \code{nSims} grows, because the
line search simply searches harder. Validating costs one simulation and one accept or
reject, and puts the last entry of \code{f_vals} on the same footing as the rest of it.

The same footing is not, however, an unbiased one. \code{evaluate_improvement} accepts a
step only when the fresh estimate is low enough, so every entry in \code{f_vals} has passed
a filter that favours a lucky ensemble. On study1 at \code{nSims = 100} the contrast at one
fixed parameter vector varied from 0.12 to 6.01 over twelve independent ensembles, and
every value the optimizer reported fell at or below the smallest of those twelve.
Neither setting of this argument gives a trustworthy final number, and neither is meant
to: \code{f_vals} is a diagnostic series for watching a fit progress, not a measure of how
good the fit is. What the argument changes is which steps get taken. To judge a fit on
real data, compare the pair correlation function implied by the fitted parameters
against the empirical one, rather than reading anything into the contrast values here.
A large spread in the contrast across repeat ensembles at the fitted parameters is
worth checking separately: it means \code{nSims} is too small for the accept/reject decision
itself to be meaningful.}

\item{subsection_count.main, subsection_count.prefit}{Points per line search grid inside
\code{\link{trust_step}}, at least 5, for the main fit and for the pre-fitting blocks
respectively.}

\item{line_iterations.main, line_iterations.prefit}{How many times each line search
refines its grid, for the main fit and for the pre-fitting blocks. Each round brackets
the current minimum two points out on either side, so the searched span shrinks by
\code{4/(subsection_count - 1)} per round and the cost of a line search is
\code{(line_iterations + 1) * subsection_count} evaluations.}

\item{max.directions.main, max.directions.prefit}{How many times \code{\link{trust_step}}
may update its conjugate direction set, for the main fit and for the pre-fitting blocks.
\code{NULL} means the original \code{2*length(parFreeIndex) - 1} (one fewer than \code{2*length},
because the original loop counted from 1 and stopped \emph{before} \code{2*n}).

These are split because the two phases want opposite things. Pre-fitting cycles between
a \verb{(log kappa, log omega)} block and a \verb{log mu} block, and cycling quickly beats solving
either one exactly, so a small value is right there; it also costs nothing on the \code{mu}
block, which has one free parameter and so no direction set to update. The main fit is
where the accuracy has to come from: on study1, scoring each fit's endpoint against a
fresh 400-pattern ensemble, \code{max.directions = 1} averaged a contrast of 1.83 against
0.91 for \code{NULL} -- about twice the error for about a twelfth of the runtime.}

\item{carry.directions}{Keep \code{\link{trust_step}}'s conjugate direction set from
one iteration to the next (\code{TRUE}) instead of rebuilding it from the coordinate axes on
every step (\code{FALSE}, the previous behaviour). Accumulating the set across iterations is
the mechanism behind Powell (1964); rebuilding it discards that, and additionally makes
every step open with a line search along free parameter 1, which biases the search
toward that parameter -- measured on study1's K block, where log kappa overshot its
target by 72\% while log omega reached only a third of its own. The set is dropped back
to the axes whenever it goes singular, since a singular set has lost a search dimension.

\strong{Only turn this on together with \code{max.directions} at \code{NULL}.} The two interact: with
few direction updates per step the carried set refreshes too slowly, a stale conjugate
direction persists while the coordinate axes are shuffled out, and the search does worse
than if it had restarted. Measured on study1's K block (12 seeds, 10 iterations), median
error against the target movement was 0.594 at \code{max.directions = 1} without carrying,
0.646 at \code{max.directions = 1} \emph{with} carrying, 0.372 at \code{NULL} without, and \strong{0.176 at
\code{NULL} with}. Hence the default is \code{FALSE}, so that the default \code{max.directions = 1}
does not silently land in the worst of those four.}

\item{carry.ess_bounds}{Keep the parameter values at which the importance sampling was
seen to break down from one trust region iteration to the next, when the iteration in
between was \emph{rejected}. A rejected step leaves both the iterate and the ensemble
untouched, so the region the ensemble supports is exactly the one the previous step
mapped, and re-deriving it costs evaluations to learn what is already known. Only the
trust radius has changed. The bounds are dropped whenever a step is accepted, since the
iterate has moved and a fresh ensemble has been simulated there, and
\code{\link{ess_bounds_carried}} additionally refuses any set whose centre does not
match the iterate about to be searched from.

This is worth much more to \code{method = "quadratic"} than to \code{"conjugate"}. The quadratic
step opens by bisecting for the breakdown along each coordinate, and carried bounds let
those searches discard the part of each bracket beyond a crossing already located
without evaluating there; the conjugate step only screens individual line search points,
and on a shrunken region most of those were going to be inside the mapped region anyway.
Re-running a step at the same centre and ensemble with the radius reduced, over 4
ensembles at \code{nSims = 300}, the evaluations it saved were

\tabular{lrr}{
\tab quadratic \tab conjugate \cr
\code{delta} 0.20 to 0.18 \tab 28-35\% \tab 0-2\% \cr
\code{delta} 0.20 to 0.16 \tab 19-38\% \tab 1-6\% \cr
\code{delta} 0.20 to 0.10 \tab 0-11\%  \tab 0\%
}

The gentler the reduction, the more of the previous step's mapping still applies, and
the more there is to save. A step that halves the radius is searching a region small
enough that little of what was learned at the old radius bears on it.

It costs nothing in accuracy: the iterate was identical with and without the carried
bounds in all 24 of those comparisons, and it has to be, since a point the bounds skip
is one the search would have evaluated and rejected. What carrying changes is only
whether it is paid for.}

\item{method.main, method.prefit}{Which \code{\link{trust_step}} method to use, for the
main fit and for the pre-fitting blocks respectively. \code{"conjugate"} (the default, and
the original behaviour) sweeps conjugate direction line searches in the manner of
Powell (1964); \code{"quadratic"} fits a quadratic model to a set of interpolation points and
minimises that, in the manner of UOBYQA (Powell 2002). See \code{\link{trust_step}} for
what separates them, and \code{\link{trust_step_quadratic}} for what the second one has
to do about the effective sample size requirement that the first can ignore.

They are split for the same reason the line search settings are: the pre-fitting blocks
have one and two free parameters, where a quadratic model needs only three and six
interpolation points, while the main fit has three and needs ten. Nothing has been
measured about which method suits which phase.}

\item{interp_fraction.main, interp_fraction.prefit}{How much of the supported box
\code{\link{trust_step_quadratic}} spreads its interpolation points over, for the main
fit and for the pre-fitting blocks. Ignored when the corresponding \code{method} is
\code{"conjugate"}. See \code{\link{trust_step_quadratic}}, where this is the argument that
most affects how good the resulting step is.}

\item{bisection_iterations.main, bisection_iterations.prefit}{Bisection steps per ray
search inside \code{\link{trust_step_quadratic}}, for the main fit and for the
pre-fitting blocks. Ignored when the corresponding \code{method} is \code{"conjugate"}.}

\item{tolPrefitLoops}{Stop pre-fitting once one full cycle moves the parameters by less
than this, and go on to the main fit for the final adjustments. The distance is
Euclidean, on the log scale the optimizer works on, taken over every coordinate in
\code{parFreeIndex} between the vector entering the cycle and the vector leaving it. Set to 0
to disable the check and always run \code{maxPrefitLoops} cycles.

Pre-fitting is a block-coordinate warm start, so the useful question is whether it is
still making progress, not whether it has converged; a tolerance is the natural way to
ask that, since the cycles settle geometrically and a fixed count cannot know where.
Measured over study1's saved fits (200 fits per ensemble size, four cycles each), the
median distance moved per cycle ran 0.93, 0.21, 0.08, 0.03 at \code{nSims = 10000} and
0.88, 0.25, 0.13, 0.07 at \code{nSims = 500}. Replaying this rule over those same movements,
the default of 0.05 stops at cycle 3 or 4 for 86\% of fits at \code{nSims = 10000} (14\% and
72\%) and 83\% at \code{nSims = 5000} (27\% and 56\%), leaving the rest to \code{maxPrefitLoops}.

Two things to watch. A small ensemble makes the blocks noisy enough to stop early by
accident: at \code{nSims = 500} the same tolerance ends 3\% of fits after the \emph{first} cycle
and another 5\% after the second, which at a median first-cycle movement of 0.88 is
plainly the noise and not a settled fit. And tightening much below 0.01 is not useful
in the other direction, because by then the cycle-to-cycle movement is the sampling
error of the block fits rather than progress, so the tolerance simply never fires and
\code{maxPrefitLoops} decides after all.}

\item{maxPrefitLoops}{Most pre-fitting cycles to run, where one cycle is a pure
K-function fit of \verb{(log kappa, log omega)} followed by a fit of \verb{log mu} alone. This is
a cap on \code{tolPrefitLoops} rather than the usual stopping rule, and the default of 6 sits
above where that tolerance normally fires so that it rarely binds -- on study1 it would
have caught the 14\% of fits at \code{nSims = 10000} that the tolerance did not. Ignored when
\code{max.iter.prefit} is 0.}

\item{max.iter.prefit}{How many iterations to do at most inside each pre-fitting block.
Set to 0 to skip pre-fitting altogether.}

\item{tol}{Convergence tolerance}

\item{max.iter}{Maximum iterations}

\item{printProgress}{Set to TRUE to get updates on progress while running}
}
\value{
A list with \code{params} (the matrix of iterates, one row per step),
\code{f_vals} (the contrast value at each iterate), \code{update_evals} (the per-step
\code{\link{trust_step}} and \code{\link{evaluate_improvement}} output),
\code{prefitResults} (the same three quantities for each pre-fitting block, or \code{NULL}
when no pre-fitting was done), and \code{prefitMoves} (the distance each pre-fitting cycle
moved the parameters, the series \code{tolPrefitLoops} is tested against).

\code{prefitResults} and \code{prefitMoves} are trimmed to the number of cycles that actually
ran, so \code{length(prefitMoves)} is that count and its last entry says which rule stopped
the pre-fitting: below \code{tolPrefitLoops} means the tolerance fired, at or above it means
\code{maxPrefitLoops} bound. Fits in the same study can therefore carry different numbers of
pre-fitting cycles, which is worth remembering when tabulating them by cycle.
}
\description{
Minimum contrast estimation using a derivative-free trust region approach. Each
iteration simulates an ensemble at the current parameters (see
\code{\link{simulation_step}}), minimises the contrast function over the trust
region by re-weighting that one ensemble with importance sampling (see
\code{\link{trust_step}}), and then re-simulates at the candidate to decide whether
to accept the step (see \code{\link{evaluate_improvement}}).

Before the main fit, the parameters can optionally be pre-fitted in blocks: first
\verb{(log kappa, log omega)} against the K-function alone, then \verb{log mu} against the full
contrast function. That cycle repeats until it stops moving the parameters -- until one
cycle shifts them by less than \code{tolPrefitLoops} -- or until \code{maxPrefitLoops} cycles have
run, whichever comes first. Pre-fitting is skipped entirely when \code{max.iter.prefit} is 0.
}
