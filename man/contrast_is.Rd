% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/min_contrast_evaluation.R
\name{contrast_is}
\alias{contrast_is}
\title{Calculate contrast function}
\usage{
contrast_is(
  simStepRes,
  params_0,
  params_new,
  rho_hat,
  K_hat,
  normalized = FALSE,
  wq = c(1000, 1/4),
  parallel_IS_weights = TRUE,
  daughter_kernel_cache = NULL
)
}
\arguments{
\item{simStepRes}{Result from simulation step, see the 'simulation_step' function}

\item{params_0}{Parameters used in simulation of the point pattern}

\item{params_new}{Target parameters}

\item{rho_hat}{Estimated intensity for data}

\item{K_hat}{Estimated K-function for data}

\item{normalized}{Normalize IS weights?}

\item{wq}{Weights for contrast function}

\item{parallel_IS_weights}{Set to TRUE to use parallel computing for the importance weights}

\item{daughter_kernel_cache}{Optional cache passed through to \code{\link{importance_sampling_weigths}};
see that function for details. Pass the \code{daughter_kernel_cache} from this call's return value into
the next call to reuse cached work when \code{params_new}'s omega is unchanged. When \code{NULL}, the cache
\code{simStepRes} already carries from its own simulation step is used instead of starting cold.}
}
\value{
A list with \code{f_est} (the contrast value) and \code{daughter_kernel_cache} (to be passed back
into the next call for reuse).
}
\description{
Take the estimated K-function and intensity,
together with the K-function and intensity for the parameters,
and calculates the contrast fucntion.
}
