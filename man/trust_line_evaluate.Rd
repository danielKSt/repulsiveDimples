% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trust_step.R
\name{trust_line_evaluate}
\alias{trust_line_evaluate}
\title{Evaluate the trust function along one grid of line search steps}
\usage{
trust_line_evaluate(
  alpha_k,
  p_k,
  x_k,
  trust_function,
  ess_bounds,
  eta_trust,
  nSims
)
}
\arguments{
\item{alpha_k}{Step lengths to evaluate along \code{p_k}.}

\item{p_k}{Search direction.}

\item{x_k}{Search starting point.}

\item{trust_function}{Trust function, returning a list with \code{f_est} and \code{ess}.}

\item{ess_bounds}{Per-parameter breakdown bounds, from \code{\link{ess_bounds_init}}.}

\item{eta_trust}{Smallest \code{ess/nSims} an iterate is allowed to have.}

\item{nSims}{Number of simulations the ensemble behind \code{trust_function} holds.}
}
\value{
A list with \code{z_k} (the evaluated points, one per column), \code{f_vals} (the
contrast estimate at each, \code{NA} where the point was ruled out) and \code{ess_bounds}.
}
\description{
Walks the points \code{x_k + alpha*p_k} and returns the contrast estimate at each, \code{NA}
wherever the ESS requirement rules the point out. A point already outside \code{ess_bounds}
is skipped before \code{trust_function} is called at all; a point that is evaluated and
then fails is added to the bounds.

The points are visited outwards from \code{x_k} rather than in grid order, i.e. by
increasing \code{abs(alpha_k)}. \code{x_k} is known to satisfy the requirement and the effective
sample size only falls off as the line runs away from it, so walking outwards meets
the breakdown on each side once and prunes the rest of that side, instead of starting
at one end of the line and paying for every ruled-out point before reaching the part
that is still usable.
}
