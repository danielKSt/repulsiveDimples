% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/min_contrast_trust_region.R
\name{trust_region_loop}
\alias{trust_region_loop}
\title{One trust region fit over a subset of the parameters}
\usage{
trust_region_loop(
  params,
  parFreeIndex,
  repRange,
  rho_hat,
  K_hat,
  xlims,
  ylims,
  nSims,
  deltaInit,
  eta,
  deltaMax,
  deltaMin = 1e-04,
  wq = c(1000, 1/4),
  normalized = FALSE,
  tol = 10^-8,
  max.iter = 1000,
  printProgress = FALSE,
  label = "trust region"
)
}
\arguments{
\item{params}{Full log-scale parameter vector \verb{(log kappa, log omega, log mu)} to
start from. Entries outside \code{parFreeIndex} are held fixed throughout.}

\item{parFreeIndex}{Indices of \code{params} to estimate.}

\item{repRange}{Repulsion range.}

\item{rho_hat}{Estimated intensity for data.}

\item{K_hat}{Estimated K-function for data.}

\item{xlims}{Simulation window x limits.}

\item{ylims}{Simulation window y limits.}

\item{nSims}{Number of simulations per iteration.}

\item{deltaInit}{Initial trust radius.}

\item{eta}{Trust region parameter.}

\item{deltaMax}{Greatest trust region radius allowed.}

\item{deltaMin}{Convergence tolerance for trust region radius.}

\item{wq}{Weights for contrast function.}

\item{normalized}{Normalize IS weights?}

\item{tol}{Convergence tolerance.}

\item{max.iter}{Maximum iterations.}

\item{printProgress}{Set to TRUE to get updates on progress while running.}

\item{label}{Name for this fit, used only in the progress messages.}
}
\value{
A list with \code{params} (the full parameter vector at the final iterate, with the
fixed entries untouched), \code{x_seq} (matrix of free-coordinate iterates, one row per
step), \code{f_vals} (contrast value at each iterate) and \code{update_evals} (the
\code{\link{trust_step}} and \code{\link{evaluate_improvement}} output for each step).
}
\description{
Runs the trust region iteration to convergence over the coordinates named by
\code{parFreeIndex}, holding the remaining entries of \code{params} fixed. This is the single
loop behind \code{\link{min_contrast_trust_region}}: the pre-fitting blocks and the
main fit differ only in which coordinates are free and how the contrast function is
weighted, so they are the same call with different \code{parFreeIndex} and \code{wq}.

Each iteration simulates an ensemble at the current parameters, minimises the
importance sampling estimate of the contrast function over the trust region with
\code{\link{trust_step}}, re-simulates at the candidate to get an independent
estimate there, and accepts or rejects the step with
\code{\link{evaluate_improvement}}. Iteration stops once a step both leaves the
iterate unchanged and is itself the trust region minimiser, or once the trust radius
has shrunk below \code{deltaMin}.
}
