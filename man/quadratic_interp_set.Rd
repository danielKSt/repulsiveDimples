% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trust_step_quadratic.R
\name{quadratic_interp_set}
\alias{quadratic_interp_set}
\title{Interpolation points for a quadratic model, and the contrast at each}
\usage{
quadratic_interp_set(
  x_0,
  box,
  delta,
  trust_function,
  eta_trust,
  nSims,
  bisection_iterations,
  state
)
}
\arguments{
\item{x_0}{Centre of the trust region and of the interpolation set.}

\item{box}{Per-coordinate offsets from \code{\link{ess_box_bisect}}.}

\item{delta}{Trust region radius.}

\item{trust_function}{Trust function, returning a list with \code{f_est} and \code{ess}.}

\item{eta_trust}{Smallest \code{ess/nSims} an iterate is allowed to have.}

\item{nSims}{Number of simulations the ensemble behind \code{trust_function} holds.}

\item{bisection_iterations}{Bisection steps per repair.}

\item{state}{A list with \code{ess_bounds} and \code{evals}, threaded through the step.}
}
\value{
A list with \code{S} (the offsets from \code{x_0} actually used, one row per point),
\code{f} (the contrast at each, \code{NA} where no feasible position was found) and the updated
\code{state}.
}
\description{
Places the \code{(n+1)(n+2)/2} points a quadratic in \code{n} variables is determined by: \code{x_0}
itself, one point out along each coordinate in each direction, and one point per pair
of coordinates moving both at once. The axial points fix the constant, the gradient and
the diagonal of the Hessian; the pair points fix its off-diagonal entries.

Points are taken to the edge of the box, so the model is fitted over the whole region it
will then be minimised in. Any point the ensemble turns out not to support -- in
practice the pair points, which is exactly where the box over-states the region -- is
pulled back along its own ray from \code{x_0} until it passes, rather than dropped, since a
dropped point leaves the interpolation system underdetermined.

The points are visited in order of their omega coordinate. \code{trust_function} keeps the
daughter kernel sums for one omega at a time and recomputing them costs about seven
times an evaluation that does not, so the points that share an omega -- the two axial
points of every other coordinate, in particular -- are cheaper taken together than
interleaved with points that move it. The saving is partial rather than total, since a
point that needs repair walks its own ray and moves omega again on the way.
}
