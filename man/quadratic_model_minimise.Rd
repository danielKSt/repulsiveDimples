% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trust_step_quadratic.R
\name{quadratic_model_minimise}
\alias{quadratic_model_minimise}
\title{Minimise the quadratic model over the trust region}
\usage{
quadratic_model_minimise(model, lower, upper, delta, starts, iterations = 200)
}
\arguments{
\item{model}{The \code{g} and \code{H} from \code{\link{quadratic_model_fit}}.}

\item{lower, upper}{Per-coordinate offsets bounding the box.}

\item{delta}{Trust region radius.}

\item{starts}{Starting offsets, one row each.}

\item{iterations}{Projected gradient steps per start.}
}
\value{
The offset from \code{x_0} minimising the model over the region.
}
\description{
Projected gradient descent with backtracking, over the intersection of the box and the
ball of radius \code{delta}. Both sets are convex and both contain the centre, and a point is
returned to the intersection by scaling it onto the ball and then clipping it to the
box -- clipping only ever moves a coordinate towards zero, so it cannot push the point
back outside the ball, and one pass of the two therefore lands inside both.
}
