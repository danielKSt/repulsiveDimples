% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trust_step_quadratic.R
\name{ess_box_bisect}
\alias{ess_box_bisect}
\title{Per-coordinate box the importance sampling supports}
\usage{
ess_box_bisect(
  x_0,
  delta,
  trust_function,
  eta_trust,
  nSims,
  bisection_iterations,
  state
)
}
\arguments{
\item{x_0}{Centre of the trust region.}

\item{delta}{Trust region radius, which caps each axis: along a single coordinate the
trust region reaches exactly \code{delta}.}

\item{trust_function}{Trust function, returning a list with \code{f_est} and \code{ess}.}

\item{eta_trust}{Smallest \code{ess/nSims} an iterate is allowed to have.}

\item{nSims}{Number of simulations the ensemble behind \code{trust_function} holds.}

\item{bisection_iterations}{Bisection steps per ray search.}

\item{state}{A list with \code{ess_bounds} and \code{evals}, threaded through the step.}
}
\value{
A list with \code{box} (a list of per-coordinate \code{lower} and \code{upper} offsets from
\code{x_0}, \verb{lower <= 0 <= upper}) and the updated \code{state}.
}
\description{
Runs \code{\link{ess_ray_limit}} out along each coordinate axis in both directions,
capped at the trust radius, and returns the offsets from \code{x_0} it reached. The box is
where \code{\link{trust_step_quadratic}} places its interpolation points; see that
function on why its corners still have to be checked individually.
}
